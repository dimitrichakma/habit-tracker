<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Template. `manage.sh install` substitutes @@UV@@ / @@PROJECT_DIR@@ and
  installs the result into ~/Library/LaunchAgents/. Do not load this file
  directly.

  Telegram bot (src/bot.py). It logs in to the backend at startup and
  exits if the backend is unreachable; KeepAlive + ThrottleInterval below
  make launchd retry every 10s until the backend agent is up.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.dimitri.habit-tracker.bot</string>

    <key>ProgramArguments</key>
    <array>
        <string>@@UV@@</string>
        <string>run</string>
        <string>python</string>
        <string>-m</string>
        <string>src.bot</string>
    </array>

    <key>WorkingDirectory</key>
    <string>@@PROJECT_DIR@@</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <!-- flush logs immediately instead of block-buffering to the file -->
        <key>PYTHONUNBUFFERED</key>
        <string>1</string>
    </dict>

    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>ThrottleInterval</key>
    <integer>10</integer>
    <key>ProcessType</key>
    <string>Background</string>

    <key>StandardOutPath</key>
    <string>@@PROJECT_DIR@@/logs/bot.out.log</string>
    <key>StandardErrorPath</key>
    <string>@@PROJECT_DIR@@/logs/bot.err.log</string>
</dict>
</plist>
