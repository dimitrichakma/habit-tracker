[pytest]
# Phase 4. `evaluation/test_rag_agent.py`'s cases are `async def` (they await
# the real agent's ainvoke); `auto` lets pytest-asyncio run them with no
# per-test @pytest.mark.asyncio decoration.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# No LangSmith / tracing config here — not in use yet (deferred to a later
# observability pass).
